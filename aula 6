#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <locale.h>
int main()
{
    setlocale(LC_ALL, "Portuguese");

    /* 1) escreva um programa em C para escrever a tabuada de um numero qualquer
    fornecido pelo usuário

    int cont = 1;
    int numero, rest;
    printf("Informe numero:");
    scanf("%d", &numero);
    while(cont<=10){
    rest = numero *cont;
    printf("\n%d * %d = %d", numero, cont, rest);
    cont++;
    }*/



    /*2)Para uma turma de 5 alunos, construir um programa em C que tendo como dados
    de entrada a altura e idade calcule e escreva.
    a) a idade média dos alunos;
    b) a altura média dos alunos.

    int cont = 1;
    int idade, somai = 0;
    float altura, somaa = 0, media;
    while(cont<=5){
    printf("Informe idade:");
    scanf("%d", &idade);
    printf("Informe altura", &altura);
    scanf("%f", &altura);
        somai = idade + somai;
        somaa = altura + somaa;
        cont++;
    }
    media = somai / 5;
    printf("\nIdade: %.1f",media);
    media = somaa / 5;
    printf("\nAltura: %.1f",media);*/



    /*3)Escreva um programa em C para ler um conjunto de 5 números calcular e escrever
o maior e menor valor deste conjunto
int numero;
int maior, menor, cont = 1;
while(cont <= 5){
    printf("Informe numero");
    scanf("%d", &numero);
    if(cont == 1){
        maior = numero;
        menor =numero;
    }else
    if(numero>maior)
        maior = numero;
    else
    if(numero< menor)
       menor = numero;
       cont++;
}

    printf("\nMaior numero: %d \nMenor numero:%d", maior, menor);*/



  /*4) Escreva um programa em C para calcular e escrever a série fibonacci até o vigésimo
termo.
1 1 2 3 5 8 13 21 34

int anterior = 0;
int proximo = 1;
int fibbo = 1;
int cont = 1;
while(cont <= 20){
printf("%d", fibbo);
fibbo = anterior + proximo;
anterior = proximo;
proximo = fibbo;
cont++;

Atividade 5

int cont = 1;
    float s = 0;
    char flag = '+';
    while(cont<=10){
    {
        s = s + (float)cont /(cont * cont);
        flag = '-';
    }
    else
    {
        s = s - (float)cont / (cont * cont);
        flag = '+';
    }
    cont++;
    }
    printf("\nValor: %0.3f", s);

Escrever um programa em C para ler um número calcular e escrever o seu fatorial.

setlocale(LC_ALL, "Portuguese");
    int cont = 1, num, fat = 1;
    printf("Informe numero não negativo:");
    scanf("%d", &num);
    while(cont <= num){
    fat = fat * cont;
    cont++;
    }
    printf("\n Fatorial do numero: %d! = %d", num, fat);

   

        return 0;
    }
